{{- if .Values.redis }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: tapin-redis
spec:
  replicas: 1
  selector:
    matchLabels:
      app: tapin-redis
  template:
    metadata:
      labels:
        app: tapin-redis
    spec:
      containers:
        - name: redis
          image: {{ .Values.redis.image }}
          ports:
            - containerPort: 6379
---
apiVersion: v1
kind: Service
metadata:
  name: tapin-redis
spec:
  selector:
    app: tapin-redis
  ports:
    - port: 6379
      targetPort: 6379
{{- end }}
