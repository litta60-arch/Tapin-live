# Android (Kotlin) sample notes

Use the LiveKit Android SDK (https://github.com/livekit/client-sdk-android). The Android sample should follow the same flow as iOS:
1. Call POST /token on your backend to get a LiveKit token
2. Use token to connect with LiveKit's Android SDK

This README is a pointer — add a full Android sample app as needed.
